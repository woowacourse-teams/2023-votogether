import { styled } from 'styled-components';

export const Button = styled.button`
  background-color: rgba(0, 0, 0, 0);
  color: var(--white);

  font-size: 14px;
  font-weight: 500;

  cursor: pointer;
`;
